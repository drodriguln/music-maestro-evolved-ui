/*
 *  Essentially a config file, but read in as a JSON variable to any component
 *  that imports it.
 */
export var config = {
  apiHost: "http://music-maestro-evolved-api.herokuapp.com",
  isProdEnv: true
}
